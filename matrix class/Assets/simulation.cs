using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class simulation : Matrix
{
    Matrix matrix;
    string resMatrix;
    // Start is called before the first frame update
    void Start()
    {

        //matrix = new Matrix();
        //matrix.matrix(2,2);
        
        

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
